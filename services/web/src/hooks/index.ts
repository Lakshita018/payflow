// Shared hooks barrel — export all custom hooks here.
export {
  useAuth,
  useLoginMutation,
  useRegisterMutation,
  useLogout,
  useInitAuth,
  useForgotPasswordMutation,
  useResetPasswordMutation,
} from './useAuth';
